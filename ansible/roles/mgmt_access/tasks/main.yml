---
- name: SSH v2 + local auth on VTY
  cisco.ios.ios_config:
    lines:
      - ip ssh version 2
      - line vty 0 4
      - " login local"
      - " transport input ssh"
      - " exec-timeout 30 0"
    save_when: modified
